# 安全架构（V1）

## 设计原则
- 零知识优先：服务端仅保存密文
- 用户控制密钥：在浏览器端派生与使用
- 最小权限与可审计：管理员只处理流程，不读用户私密明文

## 加密流程
1. 用户输入口令
2. 前端通过 Argon2id 派生主密钥
3. 每条记录生成随机数据密钥并使用 AES-GCM 加密
4. 服务端仅存储 `ciphertext + nonce + aad + version`

## 风险边界
- 忘记主密钥默认不可恢复
- 公开页内容仅来自用户明确勾选的公开字段
- 审计日志仅记录操作元数据
